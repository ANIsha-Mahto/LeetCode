class Solution {
    public int minBitFlips(int start, int goal) {
        int diff=start^goal;
        int cnt=0;
        while(diff>0){
            cnt+=diff&1;
            diff>>=1;
        }
        return cnt;
        
    }
}
